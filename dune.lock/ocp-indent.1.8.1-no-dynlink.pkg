(version 1.8.1-no-dynlink)

(build
 (all_platforms ((action (run dune build -p %{pkg-self:name} -j %{jobs})))))

(depends
 (all_platforms
  (ocaml dune cmdliner ocamlfind base-bytes)))

(source
 (fetch
  (url git+https://github.com/alicecaml/ocp-indent#1.8.1-no-dynlink)))

(dev)
